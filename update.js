let memo = {};
let N = 0;

class quadTree{
	this.b = {};
	function eq(f){
		else if
			(
				(f.b[0] == this.b[0] || f.b[0].eq(f.b[0])) &&
				(f.b[1] == this.b[1] || f.b[1].eq(f.b[1])) &&
				(f.b[2] == this.b[2] || f.b[2].eq(f.b[2])) &&
				(f.b[3] == this.b[3] || f.b[3].eq(f.b[3]))
			)
			return true;
	}
}

function update(){
	let startTime = new Date().getTime();
	
	alive = nxt;
	tpsAccurate = 1000 / (new Date().getTime() - startTime);
	tpsAccurate = (tpsAccurate > tps && tps != 0 ? tps : tpsAccurate);
}
